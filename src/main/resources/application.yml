spring:
  jpa:
    hibernate:
      ddl-auto: create #실행할 때마다 데이터 초기화, 테스트 시 설정값
    properties:
      hibernate:
        #         show_sql: true
        format_sql: true

  profiles:
    include: db

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
