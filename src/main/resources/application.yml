spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #         show_sql: true
        format_sql: true

  profiles:
    include: db

  redis:
    host: localhost
    port: 6379

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
