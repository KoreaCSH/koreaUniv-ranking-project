spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #         show_sql: true
        format_sql: true

  profiles:
    include: db

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
